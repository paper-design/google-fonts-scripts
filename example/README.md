# Font Picker Tester Project

- At the root folder: `bun packing`
- Copy `output/chunks/` and `output/font-bundle.json` to `example/fonts`
- If the number of chunks has changed, update imports in `pages/index.tsx`
- In `example`: `bun i` and `bun dev`
